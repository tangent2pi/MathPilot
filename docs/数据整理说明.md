# 参赛数据整理说明

> 下述 CSV/SQL 是确定性回归用的合成 fixtures：K=26、T=20、E=20、Q=80、R=20（
> 另含试点数据后合计 K=27/T=20/E=22/Q=84/R=21），不得作为比赛作品的正式内容库。
> 正式库必须从默认空内容库上传教师教学资料批次，经 OCR → KTQ → ER → 人工复核 → 发布生成。
> 本赛题提供的 5 份 PDF 是一次初始化资料集，不是内容工坊对文件数量或格式的产品限制。

## 一、附件使用情况

| 附件 | 使用 | 整理内容 |
|---|---|---|
| 01解三角形的入门题型.pdf | ✅ | 必备知识（正余弦定理/三角形个数/面积/形状/应用）→ K/T/E/Q/R；OCR 稿在数据目录 |
| 02解三角形中的中线、角平分线、高线问题.pdf | ✅ | 中线/角平分线/高线/张角/等面积法 → 题型与题目 |
| 03解三角形中的外接圆、内切圆问题及多三角形问题.pdf | ✅ | 外接圆/内切圆/多三角形拆分/公共边角转化 |
| 04解三角形中的最值、范围问题.pdf | ✅ | 角化边/边化角/基本不等式/三角函数性质/范围最值 |
| 05一些定理或模型问题.pdf | ✅ | 正切恒等式/正弦平方差/圆幂/隐圆/阿波罗尼斯圆/托勒密 |
| 高中数学AI题库模板_v1.xlsx | ✅ | 六表结构参考（ID 规则 K/T/E/R、掌握标准、补弱建议、训练顺序） |

## 二、结构化数据规模（已达赛题最低要求）

| 数据项 | 数量 | 最低要求 | 说明 |
|---|---:|---:|---|
| 知识点 | 26 | ≥25 | 5 专题 × 5 条三级知识点（module/name/掌握标准/补弱建议），另含试点 K_SSA |
| 题型 | 20 | ≥20 | 典型问法/标准步骤/评分点逐条撰写 |
| 错因 | 20 | ≥20 | 5 大类（公式误用/分类讨论不完整/对应错误/取等遗漏/运算粗心…），另含试点 2 条 |
| 题目 | 80 | ≥80 | 5 专题 × 16 道，数值经人工验算；每道含题干/答案/两评分点/测量目标（primary+secondary+prerequisite） |
| 诊断规则 | 20 | ≥20 | 触发条件→候选错因→消歧追问→适用维度（K/T）逐条对应 |
| 学生案例 | 3 | 3 | 见 `data/student_cases.md`（含逐题对话/判定/追问/双产物/快照/计划全链记录） |

> fixtures 的生成与验证：`deploy/dev/seed_gen.py` 产出 `deploy/dev/seed.sql`；默认
> `docker compose up` 只执行 `bootstrap.sql`，不会灌入这些题。仅回归测试可显式运行
> `docker compose --profile fixtures run --rm db-fixtures`。正式六表应由 PDF 入库后通过
> `deploy/dev/export-data.sh` 从数据库派生导出（CSV 不是运行时数据源）。内容工坊允许 PDF、图片、
> Word 与 PowerPoint 等教学资料；每份原件及其 OCR 产物独立固化，KTQ 则在同一批次工作区中跨资料抽取和去重。

## 三、数据文件清单

```text
data/
  knowledge_points.csv     # K=27（26+试点）派生导出，含模块/掌握标准/补弱建议
  question_types.csv       # T=20
  error_causes.csv         # E=22（20+试点 2）
  questions.csv            # Q=84（80+试点 2 题 + 历史 2 题）
  diagnosis_rules.csv      # R=21（20+试点 1）
  student_cases.csv        # 3 案例结构化摘要
  student_cases.md         # 完整案例（对话/逐题证据/实际报告/计划）
```

## 四、标签规则说明

- ID 规则：K=知识点、T=题型、E=错因、Q=题目、R=规则；前缀 + 语义缩写（如 `K_SSA`、`Q_BAS_003`）；ID 稳定后不改名（模板"ID规则"）。
- 一道题关联多个知识点：`tags` 全量标注（检索用）；**掌握归因只经 `measurement_targets`**（primary/secondary/prerequisite + evidence_rule，设计 §2.3）——一次作答不无差别写入所有 K。
- 一道题多个潜在错因：作答后经错因归因任务选择候选（≤3，**候选只来自题目关联 E 与规则**，P0-7），追问消歧；未闭合标 `unresolved`（单会话层不写画像，画像级判定归 Dream，§7.2）。
- 观测纪律（P0-6）：主答按评分点逐项写观测（met→success / not_met→failure，部分正确不再整体计失败）；探针属教学追问（teaching_only）不构成独立证据；每观测携带判定引用与证据引用。

## 五、测评与诊断规则

- **首批测评题选择**：AssessmentRun 两阶段——会话结束由教学主模型判定目标（覆盖/消歧/前置/复测/训练/迁移），传统程序选题器硬过滤（未见/可验证/有测量目标）+ 评分（§10）；题量/时间预算由服务端强制（P1）。
- **答对/答错后调整**：判定正确→关闭；部分/错误→错因归因→追问卡（≤3 轮，可跳过）→闭合或待观察；会话结束判定下一目标。
- **掌握/待观察/薄弱判定**：BKT 程序基准（OATutor 引擎 + pyBKT 校准/Roster）+ 五档状态门槛（0.4/0.8/0.95，证据不足/薄弱/学习中/可能掌握/已掌握）；提示后成功不构成独立证据。
- **作答表现映射错因**：模型主判（评分点逐项）+ 错因归因任务（候选只来自题目关联 E 与规则）+ 追问证据入 DiagnosticClaim。
- **目标分学习计划**：确定性约束排布（薄弱→补讲+低档+原难度；复测到期→延迟复测；目标差→迁移；**按每周预算与 1-4 周界排布**，P0-10）+ 大模型转写解释（不增删任务）。

## 六、学生案例

见 `data/student_cases.md`：case_001 基础薄弱 / case_002 分类讨论不全 / case_003 冲高分。
三案例为系统黄金数据全链记录（画像→逐题对话→错因追问→双产物→Dream→快照→计划），
ID 与数据库一一对应，可经报告/计划页直接查看。

## 七、质量校验

- 每字段血缘（content_field_lineage：来源片段/Agent Run/模型/Prompt/审核决定）；
- 发布前置门（P0-4）：本章节复核任务全部裁决且无 rejected；modified 裁决真正生效；
  章节包按章节绑定 chapter_id 且 contents 只收集该章节对象（P0-5）；
- 契约测试：21 schema 样例校验 + BKT/保持率/选题器/计划排布/Dream Validator 单测；
- 侧车对拍：pyBKT Roster 与 TS 引擎差异 < 1e-9，supersede 重放与 DB 语义一致（P0-8）。
