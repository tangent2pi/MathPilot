# Core routing example

Inspect metadata first for video/audio. Use `visualize` for documents, `read_image` for an image, and `crop`/`draw_bbox` only when a precise region helps. Keep every derived file below `/workspace/output` or `/workspace/tmp`.
