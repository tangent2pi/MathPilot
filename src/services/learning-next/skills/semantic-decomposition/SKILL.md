---
name: semantic-decomposition
description: Propose child tasks from the host-provided allowlist for a compound goal.
---

# Semantic decomposition

Decompose only into the supplied allowlisted task types. The host owns child workflow IDs, models, permissions, tenant binding and cancellation. Do not propose code, shell commands, credentials or arbitrary tools.
