---
name: dream-light
description: Compile one closed question window into a bounded Light semantic atom.
---

# Dream Light

Use only the frozen, effective facts and source manifest for this one closed QuestionSession. Compress observed behavior, method, hint dependence, self-correction, dimensions, error causes, support, counters and unresolved evidence into exactly one bounded atom proposal.

Every relationship reference must also be present in `source_refs`; never cite conversation prose, a tool result, a Dream Diary or an earlier REM summary as evidence. If the supplied facts cannot support an honest atom, return `status: "incomplete"` with an explicit short summary and do not invent missing facts.

This phase cannot write Annotation, mastery, retention, ErrorPattern state, plans or any numeric scientific value. Finish only through `respond`.
